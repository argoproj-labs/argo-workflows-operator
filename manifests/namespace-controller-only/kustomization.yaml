apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - https://github.com/argoproj/argo/manifests/base/workflow-controller?ref=stable
  - https://github.com/argoproj/argo/manifests/namespace-install/workflow-controller-rbac?ref=stable

patchesStrategicMerge:
  - ./overlays/workflow-controller-configmap.yaml

patchesJson6902:
  - target:
      version: v1
      group: apps
      kind: Deployment
      name: workflow-controller
    path: ./overlays/workflow-controller-deployment.yaml
